# Install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu124
#
# The --extra-index-url pulls torch with CUDA 12.4 support; other packages from PyPI.

# Core ML
torch>=2.0
transformers>=4.40
tokenizers

# Topology
ripser==0.6.14
giotto-tda
networkx
# ripserplusplus is the GPU ripser the Kushnareva paper used. PyPI's
# ripserplusplus 1.1.2 doesn't compile against CUDA 12+ (thrust API
# changed); upstream master has the fix (merged via PR #14 in 2023) but
# was never re-released. Pinned to that fixed commit. Build requires
# nvcc + a CUDA toolkit matching torch's CUDA major version.
ripserplusplus @ git+https://github.com/simonzhang00/ripser-plusplus.git@30243c0c752de26d7fdf6e41f08bf7b840ca4744

# Distributional semantics
gensim==4.4.0

# Stats and metrics
scikit-learn
scipy
numpy<2
pandas

# Visualization
matplotlib
seaborn

# Notebooks
jupyter
jupyterlab
ipykernel

# Utilities
tqdm
pyyaml

# Phase 1 KWIC — lemma matching
pymorphy3
spacy>=3.7
# spaCy models are NOT installed via pip; download them separately:
#   python -m spacy download en_core_web_md
#   python -m spacy download es_core_news_md
# Do NOT pin model versions here — spaCy manages model installs via its own CLI.
